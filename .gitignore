/.claude
/.env
/data/hometasks.db
server.log
nul
tuya_devices.json

# Claude Code local settings (per-user, nu se urcă)
.claude/settings.local.json

# Frontend SPA (Vite)
# node_modules/ fără prefix: prinde și frontend/node_modules/, și node_modules/
# creat în rădăcină când vitest e rulat de acolo (cache .vite/vitest).
node_modules/
frontend/dist/
frontend/.vite/
frontend/*.log
/frontend/coverage

# Python bytecode (oriunde în repo)
__pycache__/
*.py[cod]

# Virtualenv local (pe RPi trăiește separat, în afara repo-ului)
venv/
.venv/

# Artefacte de testare (pytest / coverage)
.pytest_cache/
.coverage
.coverage.*
htmlcov/

# Daily server log files (written by src/logbuf.py)
logs/
